/// @description vectorCalc(vectorForce, vectorAngle);
/// @function  vectorCalc
/// @param vectorForce
/// @param vectorAngle

var array

array[0] = (argument0 / dsin(90))*dsin(argument1)
array[1] = (argument0 / dsin(90))*dsin((90-argument1))

return array

